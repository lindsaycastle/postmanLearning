$kind: http-request
url: "{{baseUrl}}/orders/:orderId"
method: PATCH
headers:
  - key: Authorization
    value: Bearer:{{vault:groc_ath}}
    disabled: true
pathVariables:
  orderId: "{{orderId}}"
body:
  type: json
  content: "{\r

    \ \"comment\": \"I like turtles\"\r

    }"
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 204\", function () {\r

      \    pm.response.to.have.status(204);\r

      });"
    language: text/javascript
order: 16000
