$kind: http-request
url: "{{baseUrl}}/1/cards/{{CardId}}?key={{vault:Trello_API_key}}&token={{vault:Trello_token}}&idList={{DoneListID}}"
method: PUT
headers:
  Accept: application/json
queryParams:
  key: "{{vault:Trello_API_key}}"
  token: "{{vault:Trello_token}}"
  idList: "{{DoneListID}}"
order: 4000
